import styled from 'styled-components'
import { CustomButton } from '../components/CustomButton'

export const CustomButtonHome = styled(CustomButton)`
        margin-left: 30px;
        margin-bottom: 13px;
        `
